# Data

Datasets are local and ignored by Git. As the project grows, use subdirectories
such as `raw/`, `interim/`, and `processed/`, and record the source, license,
checksum, and preparation command for every dataset.

Never modify raw source data in place. Generate a new processed version instead.
