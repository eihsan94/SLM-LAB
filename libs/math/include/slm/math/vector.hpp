#pragma once

#include <vector>

namespace slm::math {

// A named alias keeps lesson equations readable while the learner is still
// implementing vector operations from scratch.
using Vector = std::vector<float>;

}  // namespace slm::math
